<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mehungry.feedme">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDBc3FgMbWEcuDq7jj6fHZqj1EYHhhSUUU" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".SearchRestaurantActivity" />
        <activity android:name=".SearchResultsActivity" />
        <activity android:name=".ViewOptionsActivity" />
        <activity android:name=".ViewHelpActivity" />
        <activity android:name=".ViewProfileActivity" />
        <activity android:name=".ViewNotificationActivity" />
        <activity android:name=".ViewSecurityActivity" />
        <activity android:name=".ViewAccountSettingsActivity" />
        <activity android:name=".GetRestaurantsAsyncActivity"></activity>
    </application>

</manifest>